// REducer

const INITIAL_STATE = {};

//Actions

export default function repositories(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }

  console.log(action);
  return state;
}
